import { default as author } from './author'
import { default as post } from './post'
import { default as page } from './page'

// The order of these exports determines the order
// of the links in the sidebar
export { post, author, page }
